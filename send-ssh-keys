---

  - name: Sending ssh keys
    hosts: all
    become: yes

    tasks:

      - name:
        authorized_key:
          user: supervisor
          state: present
          key: "{{ lookup('file', '~/.ssh/id_rsa.pub') }}"
