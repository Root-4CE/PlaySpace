---

  - name: Sending ssh keys
    hosts: all
    become: yes
    become_user: supervisor

    tasks:

      - name:
        authorized_key:
          user: supervisor
          state: present
          key: "{{ lookup('file', 'supervisor@192.168.17.3:~/.ssh/id_rsa.pub') }}"
