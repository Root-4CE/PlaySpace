- name: Play 1
  hosts: router
  
  tasks:

    - name: set ip forwarding = 1
      ansible.posix.sysctl:
        name: net.ipv4.ip_forward
        value: '1'
        sysctl_set: yes
        state: present
        reload: yes

    - name: iptables - 
       
         
